{
  "$schema": "https://apify.com/schemas/v1/input.ide.json",
  "title": "Scrape data from a web page",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "url": {
      "title": "URL of the brand website",
      "type": "string",
      "description": "The target website URL used to infer product/industry and auto-generate search queries",
      "editor": "textfield",
      "prefill": "https://www.apify.com/",
      "default": "https://www.apify.com/"
    },
    "search_queries": {
      "title": "Search Queries (Optional)",
      "type": "array",
      "description": "Optional list of search queries. If omitted, CiteShift auto-generates queries from the website.",
      "editor": "stringList",
      "default": [],
      "items": {
        "type": "string"
      },
      "minItems": 0
    }
  },
  "required": ["url"]
}
